# Grader-RL signal run — handoff, 2026-08-30

Repo: `~/claudecode/opsd` (local) + `/scratch/gpfs/ARORA/arora/opsd` (della,
file-sync not git). Branch `contrastive-opsd`, pushed to org repo
`princeton-pli/rethinking-opsd-for-thinking-models` (write access confirmed
2026-08-28; `fix-countdown-grader` branch holds the clean grader fix for
Simran). Prior context: HANDOFF_2026-08-27.md, SPEC_grader_rl.md (the spec
governs; all thresholds/interpretations were pre-registered).

## What ran (2026-08-28/29)

1. **Hard-Countdown data**: two generators. Primary = pattern-balanced per
   arXiv 2512.01775 (`pilot/gen_countdown_balanced.py`, vendored from
   RL-skill-comp@c32724b; 812 repeated-letter n=6 patterns need the
   distinct-letter instantiation — CR-caught). Secondary = tree slice
   (`pilot/gen_hard_countdown.py`, integer intermediates, signed-sum
   rejected). 3k instances each, n∈{5,6}.
2. **Gate harvests+probes** (fixed grader, Qwen3-4B, k=8):
   | set | 8/8 | 0/8 | usable | adj-when-copy nothink/think |
   |---|---|---|---|---|
   | 3-4 num (old) | 75.2% | 2.4% | 7.9% | 75.4/90.0 |
   | tree slice | 43.2% | 13.2% | 36.0% | 65.9/80.8 |
   | balanced | 30.3% | 16.9% | **46.1%** | **65.4/81.1** |
   Both gates PASSED (≥25% usable, ≥65% adj). Neglabel backfire replicated
   3x (worst on hard: picks known-wrong 38-41%).
3. **Grader-RL signal run** (job 13156495): verl 0.7.0 (rlvr venv in
   della-post-training), Qwen3-4B LoRA r=32 α=64, GRPO n=8, lr 1e-6,
   3,122 episodes (pair context + thinking prefix U[0,½], H1-disjoint),
   outcome-only vendored reward. 47/48 steps in 6h; ckpt step 40. Healthy
   throughout: KL≈5e-4, entropy flat, NO length collapse.

## Results (all on 92 held-out pairs / same-300-problem probes)

* **RL effect: null by pre-registration.** Pair-margin +0.0001 nats; probe
  cells within noise. Weight-level: ΔW absmax ~2e-5 (below bf16 ULP) — the
  dose was homeopathic (lr 1e-6 × 40 steps), pipeline itself validated.
* **HEADLINE (base-model property): the forward-pass margin OPSD distills
  from is ~zero.** Whole-trace mean per-token margin of x⁺ vs x⁻ under
  pair context: −0.0018 nats, 46.7% positive — no correctness signal,
  and WORSE than bare context (+0.0199). Same model adjudicates these
  pairs generatively at 65–81%. The §3 insight, now measured in OPSD's
  exact regime on the task where generation-side adjudication works.
* **Answer-token margin refines it**: +0.648 nats mean but only 52.2%
  positive (heavy right tail — a minority of pairs carries it); bare
  context +0.603, so the pair adds only ~0.05. Correctness signal exists
  in the conditionals but is (a) concentrated on answer tokens (~1500x
  dilution in a uniform distillation loss) and (b) mostly the model's own
  solving prior, not pair-reading.
* Transfer cells (Sanjeev's grading→solving hypothesis): none/think
  0.422→0.458, none/nothink 0.033→0.043 — right sign, ~1.3σ, not evidence
  at this dose.

## LATE ADDITION (2026-08-30 ~01:00) — the sparse-signal result

Sanjeev's hypothesis (near-zero trace-mean is compatible with concentrated
learning-relevant signal) tested directly on wrong rollouts
(`rl/eval_sparse_signal.py`, results `rl/sparse_signal_base.jsonl`):

* **Answer-slot steering: pair-specific increment +0.925 nats, 29σ,
  positive on 90.9% of 922 wrong rollouts.** At the wrong trace's own
  \boxed{ slot, pair context moves the teacher toward the correct
  expression by ~a nat (bare −1.43 → pair −0.50). The contrastive signal
  is LARGE exactly where the outcome-gated loss acts.
* **KL localization**: the pair perturbs conditionals broadly
  (0.4–0.9 nats/token, early-mid-trace prose heavy; math tokens carry
  20.2% of KL mass on 42.3% of tokens) — uniform distillation spends ~99%
  of gradient on pair-conditioned STYLE, burying the slot signal ~100:1.
* Reconciliation of all margin measurements: whole-trace ~0; clean-
  continuation answer tokens +0.65 mostly solving-prior (pair +0.05);
  wrong-trace answer slot pair-specific +0.92 — decisive where the model
  cannot solve alone.

**Design implication (now measured, not speculative): token-weighted
contrastive-OPSD** — distill on wrong rollouts with loss weight
concentrated on the answer region — has a 29σ mechanism. Grader-RL's
target likewise sharpens to amplifying the slot increment.

## Open decisions (Sanjeev)

(a) hotter RL (lr ~1e-5, 3+ epochs, track answer-token margin as target);
(b) token-weighted contrastive-OPSD (upweight answer region in the
distillation loss) — design change, needs his call; (c) both.
Also parked: mixed-diet arm for the transfer hypothesis (pre-registered in
spec); Simran confound re-grade (blocked on path to their Countdown eval
generations — not under any readable /scratch path; branch
`fix-countdown-grader` has the fix + instructions).

## Operational gotchas (new since 08-27)

* **rlvr venv verl is 0.7.0, NOT the RL-skill-comp pinned commit**: sync
  rollout retired; tokenization lives in the AgentLoop; verbatim
  mid-<think> prompts need `rl/prefix_dataset.py` + identity chat template
  passed as `"+data.apply_chat_template_kwargs.chat_template=\"$TPL\""`
  (hydra needs the escaped quotes; bare Jinja braces crash the parser).
* **TIKTOKEN_ENCODINGS_BASE** must point at della-post-training's
  `envs/harmony_encodings` or verl dies on compute nodes (no internet).
* **The step-40 adapter is below bf16 ULP**: never evaluate it through a
  bf16 merge; use fp32 forward (margin eval) or vLLM enable_lora (probe).
  `eval_adapter/` (peft, correct α=64) and `eval_merged/` exist under
  rl/checkpoints/grader_grpo_lora_v1/; verl's own model_merger does NOT
  merge LoRA (saves base + adapter with broken α=0 config).
* pli-c rejects GPU-less jobs; use `-p cpu` for CPU-only slurm work.
* 16h GPU jobs: harvest parquets are written last-thing before the probe
  stage — the idempotent `[ -f parquet ]` guards make TIMEOUT+resubmit
  cheap. cd_bal's parquet landed 1 minute before its kill.
* VPN dropped 3x during this stretch; file-existence watchers
  (`until ssh ... ls file; do sleep; done`) are drop-proof, squeue-empty
  watchers are not.
